import ws from "ws";

// Handles authentication of nodeId, both for the server and client.

export async function getLocalNodeId(): Promise<string> {

}


export function getNodeId(webSocket: ws.WebSocket): string {
    new ws.WebSocket()

    todonext;
    // Force the websocket to connect with peer certificates, and get the certificate from it, somehow?
    webSocket.on("open", e => {
        
    });
}